import { ProjectEndDatePipe } from './project-end-date.pipe';

describe('ProjectEndDatePipe', () => {
  it('create an instance', () => {
    const pipe = new ProjectEndDatePipe();
    expect(pipe).toBeTruthy();
  });
});
